  spring:
  datasource:
    url: jdbc:mariadb://startream.iptime.org:4306/test
    username: test
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  logging:
    level:
      org.hibernate:
        type.descriptor.sql: trace #show parameter binding
        SQL: DEBUG
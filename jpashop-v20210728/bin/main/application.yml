spring:
  datasource:
    url: jdbc:mariadb://startream.iptime.org:4306/test?characterEncoding=UTF-8&serverTimezone=UTC
    username: test
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace